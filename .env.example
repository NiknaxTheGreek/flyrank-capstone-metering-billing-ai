# Local development configuration — placeholders only. Never commit a real .env file.
APP_ENV=development

# PostgreSQL (planned FastAPI/SQLAlchemy application database)
DATABASE_URL=postgresql+psycopg://username:password@localhost:5432/flyrank_metering

# Stripe test mode only
STRIPE_SECRET_KEY=sk_test_replace_with_test_key
STRIPE_WEBHOOK_SECRET=whsec_replace_with_test_webhook_secret
STRIPE_PRO_PRICE_ID=price_replace_with_test_price_id